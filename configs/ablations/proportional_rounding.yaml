inherits: ../dico/dico_cd_da_r8_covra_full_experimental.yaml

experiment_name: ablation_proportional_rounding
ablation:
  id: proportional_rounding
  reference_config: configs/dico/dico_cd_da_r8_covra_full_experimental.yaml
  mechanism_group: budget_solver
  single_factor: solver
  expected_difference: Replace DP integer budget solver with proportional rounding control under the same utility curves and rank bounds.
preallocation:
  solver: proportional_rounding
