inherits: ../dico/dico_cd_da_r8_covra_full_experimental.yaml

experiment_name: ablation_grouped_only
ablation:
  id: grouped_only
  reference_config: configs/dico/dico_cd_da_r8_covra_full_experimental.yaml
  mechanism_group: sketch_blocks
  single_factor: sketch_block_mode
  expected_difference: Use only grouped response sketch blocks and disable the global sketch block.
preallocation:
  sketch_block_mode: grouped_only
