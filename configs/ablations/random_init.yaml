inherits: ../dico/dico_cd_da_r8_covra_full_experimental.yaml

experiment_name: ablation_random_init
ablation:
  id: random_init
  reference_config: configs/dico/dico_cd_da_r8_covra_full_experimental.yaml
  mechanism_group: initialization
  single_factor: init_mode
  controlled_difference_fields:
    - dico.init.mode
  expected_difference: General random-initialization control for the final CovRA path; kept distinct from the E06 CovRA-rank+Random named cell by metadata.
dico:
  init:
    mode: kaiming_zero_B
