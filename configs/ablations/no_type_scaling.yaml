inherits: ../dico/dico_cd_da_r8_covra_full_experimental.yaml

experiment_name: ablation_no_type_scaling
ablation:
  id: no_type_scaling
  reference_config: configs/dico/dico_cd_da_r8_covra_full_experimental.yaml
  mechanism_group: utility_scaling
  single_factor: type_scaling
  expected_difference: Disable module-type median scaling while preserving sign split and log compression.
preallocation:
  type_scaling: false
