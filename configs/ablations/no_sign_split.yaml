inherits: ../dico/dico_cd_da_r8_covra_full_experimental.yaml

experiment_name: ablation_no_sign_split
ablation:
  id: no_sign_split
  reference_config: configs/dico/dico_cd_da_r8_covra_full_experimental.yaml
  mechanism_group: response_blocks
  single_factor: sign_split
  expected_difference: Disable adaptive positive/negative response block splitting while keeping candidate selection and budget solver unchanged.
preallocation:
  sign_split: false
