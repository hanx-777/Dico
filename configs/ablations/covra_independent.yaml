inherits: ../dico/dico_cd_da_r8_covra_full_experimental.yaml

experiment_name: ablation_covra_independent
ablation:
  id: covra_independent
  reference_config: configs/dico/dico_cd_da_r8_covra_full_experimental.yaml
  mechanism_group: conditional_value
  single_factor: allocation_method
  expected_difference: Replace conditional residual gains with fixed independent candidate-response energy; all other CovRA protocol fields are inherited.
preallocation:
  allocation_method: covra_independent
